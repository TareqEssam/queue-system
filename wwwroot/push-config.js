// Public standard Web Push configuration. The VAPID public key is safe to expose in the browser.
// Generate a VAPID key pair for this production deployment and paste the public key here.
globalThis.QUEUE_PUSH = {
  publicVapidKey: ""
};
